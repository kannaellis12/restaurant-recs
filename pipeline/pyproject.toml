[project]
name = "reddit-restaurants-pipeline"
version = "0.1.0"
description = "Ingestion + scoring pipeline for Reddit Restaurants"
requires-python = ">=3.9"

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
